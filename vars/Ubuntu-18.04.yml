---

zentral_pkg_deps:
  - cpio
  - git
  - xmlsec1
  - python3
  - python3-pip
  - gcc
  - libffi-dev
  - libssl-dev
  - python3-dev
  - libldap2-dev
  - libsasl2-dev

gunicorn_path: /usr/local/bin/gunicorn
ssl_user: ssl-cert
ssl_dir: /etc/ssl
ssl_privatedir: /etc/ssl/private
